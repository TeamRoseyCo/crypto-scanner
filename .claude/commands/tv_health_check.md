Verify that the CDP (Chrome DevTools Protocol) connection to TradingView is live.

Run this command from the project root and report the output:

```bash
python "python-scanners/tradingview/tv_health_check.py"
```
